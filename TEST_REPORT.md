# 测试报告

运行命令：

```bash
PYTHONPATH=src pytest -q
```

验收结果：

```text
.................                                                        [100%]
18 passed
```

覆盖范围：

- 通用实体与组件序列化；
- 角色/组件查询；
- 线性、圆周和路径点运动；
- 多阵列、多接收器求解；
- 动态30帧结果；
- 平面探针维度；
- `.hpmdt` 工程往返；
- FastAPI健康接口与工作台首页；
- 工作区增删改、运行和保存。

当前未覆盖：

- 浏览器端完整E2E；
- 任务取消与异步进度；
- Three.js TransformControls；
- 大规模结果分块存储。
